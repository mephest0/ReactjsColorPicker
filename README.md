# ReactjsColorPicker
## Simple hue slider
A simple app, and my first in React. Drag the slider to change the hue of the color. Colors can be saved for later. There is also a set of pre-defined colors to choose from.

### Frameworks and libraries
Written in React with Redux.

### Running
To run in dev server, run `npm start`. To build, run `npm run-script build`.
